
spring:
  session:
    store-type: redis

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/at15?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 9999

security:
  user:
    name: root
    password: root